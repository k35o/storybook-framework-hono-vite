import type { StorybookConfig } from '../config-types.js';

export type { FrameworkOptions, StorybookConfig } from '../config-types.js';

export function defineMain(config: StorybookConfig): StorybookConfig {
  return config;
}
